spring:
  application:
    name: portfolio

  datasource:
    url: ${DB_URL:jdbc:postgresql://db.acctunyhvicrvlydhspt.supabase.co:5432/postgres?sslmode=require}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:ZKW5M8KXMvYFnRWi}
    driverClassName: org.postgresql.Driver

  
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true

app:
  frontend:
    url: "${FRONTEND_URL:http://localhost:5173}"