spring:
  application:
    name: portfolio

  datasource:
    url: ${DB_URL:jdbc:postgresql://aws-1-us-east-2.pooler.supabase.com:6543/postgres?sslmode=require}
    username: ${DB_USERNAME:postgres.acctunyhvicrvlydhspt}
    password: ${DB_PASSWORD:ZKW5M8KXMvYFnRWi}
    driverClassName: org.postgresql.Driver

  
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true

app:
  frontend:
    url: ${FRONTEND_URL:https://portfolio-dev-full.vercel.app}